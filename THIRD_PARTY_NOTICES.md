# Third-party notices

GittyGo includes Electron and its Chromium/Node.js components. Their license notices are distributed with the application under `Contents/Resources/licenses/`.

GittyGo's packaged JavaScript dependencies are:

- `proper-lockfile` — MIT
- `graceful-fs` — ISC
- `retry` — MIT
- `signal-exit` — ISC

Each dependency's license text remains included alongside that dependency in `app.asar/node_modules/`.
